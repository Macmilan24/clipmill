# generated by datamodel-codegen:
#   filename:  clipmill.analysis.manifest.v1.json

from __future__ import annotations

from enum import StrEnum
from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, conint, constr


class Coverage(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    start_ticks: conint(ge=0)
    end_ticks: conint(ge=0)
    analyzed: bool


class Sha256(RootModel[constr(pattern=r'^sha256:[0-9a-f]{64}$')]):
    root: constr(pattern=r'^sha256:[0-9a-f]{64}$')


class Kind(StrEnum):
    evidence_source_map_v1 = 'evidence.source_map.v1'
    media_ingest_manifest_v1 = 'media.ingest_manifest.v1'
    speech_vad_v1 = 'speech.vad.v1'
    speech_asr_v1 = 'speech.asr.v1'
    speech_alignment_v1 = 'speech.alignment.v1'
    speech_transcript_v1 = 'speech.transcript.v1'
    evidence_shots_v1 = 'evidence.shots.v1'
    index_transcript_v1 = 'index.transcript.v1'
    discovery_candidates_v1 = 'discovery.candidates.v1'
    ranking_set_v1 = 'ranking.set.v1'


class Stage(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    kind: Kind
    artifact_id: Sha256


class Reason(StrEnum):
    no_video = 'no_video'
    no_audio = 'no_audio'


class SkippedStage(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    kind: constr(min_length=1)
    reason: Reason


class AnalysisManifest(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['clipmill.analysis.manifest.v1']
    source_fingerprint: Sha256
    stages: list[Stage] = Field(
        ...,
        description='Every stage that produced an artifact, in the order the DAG ran them. A stage that was skipped because the source lacked the input it needs is absent rather than present with a null: a recording with no video has no shot detection, and that is a different document from one whose shot detection failed.',
        min_length=1,
    )
    coverage: Coverage = Field(
        ...,
        description='The span the analysis speaks for, which is the narrowest of what its stages examined. A consumer reading a candidate outside this range is reading a claim nobody made.',
    )
    skipped: list[SkippedStage] | None = Field(
        None,
        description="Stages the plan did not run, and why. Present so that 'this recording has no shot cuts' and 'nobody looked for shot cuts' remain distinguishable at the top level, without opening any of the artifacts below.",
    )
