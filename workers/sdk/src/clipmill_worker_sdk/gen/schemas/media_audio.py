# generated by datamodel-codegen:
#   filename:  clipmill.media.audio.v1.json

from __future__ import annotations

from enum import StrEnum
from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, conint, constr


class Codec(StrEnum):
    pcm_s16le = 'pcm_s16le'


class Sha256(RootModel[constr(pattern=r'^sha256:[0-9a-f]{64}$')]):
    root: constr(pattern=r'^sha256:[0-9a-f]{64}$')


class Coverage(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    start_ticks: conint(ge=0)
    end_ticks: conint(ge=0)


class MediaAudio(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['clipmill.media.audio.v1']
    source_fingerprint: Sha256
    file: constr(min_length=1) = Field(
        ..., description='Artifact-relative path of the WAV payload.'
    )
    codec: Codec
    sample_rate: conint(ge=1)
    channels: conint(ge=1)
    sample_count: conint(ge=0) = Field(
        ...,
        description='Frames of audio (per-channel samples), the exact payload length.',
    )
    duration_ticks: conint(ge=0) = Field(
        ..., description='Rendition duration in ticks at 1/90000.'
    )
    coverage: Coverage
