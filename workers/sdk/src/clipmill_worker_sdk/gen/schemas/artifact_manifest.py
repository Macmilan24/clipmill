# generated by datamodel-codegen:
#   filename:  clipmill.artifact.manifest.v1.json

from __future__ import annotations

from enum import Enum
from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, conint, constr


class Timebase(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    num: conint(ge=1)
    den: conint(ge=1)


class Policy(Enum):
    local_lock = 'local-lock'
    network_allowed = 'network-allowed'


class Sha256(RootModel[constr(pattern=r'^sha256:[0-9a-f]{64}$')]):
    root: constr(pattern=r'^sha256:[0-9a-f]{64}$')


class Producer(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    stage: constr(pattern=r'^[a-z][a-z0-9-]*$') = Field(
        ..., description="Stage identity, e.g. 'active-speaker'."
    )
    implementation: str = Field(
        ..., description="Implementation and version, e.g. 'loconet-adapter@1.4.0'."
    )
    model_digest: Sha256 | None = Field(
        None,
        description='Digest of the model manifest, when a model produced this artifact.',
    )


class File(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    path: constr(pattern=r'^[^/].*$') = Field(
        ...,
        description='Relative path within the artifact directory. The store additionally rejects traversal and absolute paths at write time.',
    )
    sha256: Sha256
    bytes: conint(ge=0)


class ArtifactManifest(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['clipmill.artifact.manifest.v1']
    artifact_id: Sha256 = Field(
        ...,
        description='Content address: hash over stage identity, ordered input hashes, canonical config, model manifest digest, and semantic version.',
    )
    kind: constr(pattern=r'^[a-z][a-z0-9_.]*\.v[0-9]+$') = Field(
        ..., description="Versioned artifact kind, e.g. 'evidence.active_speaker.v2'."
    )
    source_fingerprint: Sha256 = Field(
        ..., description='Fingerprint of the source media this artifact derives from.'
    )
    timebase: Timebase = Field(
        ...,
        description="The rational timebase all times in the artifact's data files are expressed against.",
    )
    producer: Producer
    inputs: list[Sha256] = Field(
        ..., description='Ordered artifact ids this artifact was derived from.'
    )
    policy: Policy = Field(
        ..., description='Network policy under which this artifact was produced.'
    )
    quality: dict[str, float] | None = Field(
        None, description='Producer-reported quality signals, e.g. {"coverage": 0.97}.'
    )
    files: list[File] = Field(
        ...,
        description="The artifact's data files, hash-verified on read.",
        min_length=1,
    )
