# generated by datamodel-codegen:
#   filename:  clipmill.media.audio_peaks.v1.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, conint, constr


class Peak(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    min: conint(ge=-32768, le=32767)
    max: conint(ge=-32768, le=32767)


class Sha256(RootModel[constr(pattern=r'^sha256:[0-9a-f]{64}$')]):
    root: constr(pattern=r'^sha256:[0-9a-f]{64}$')


class Coverage(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    start_ticks: conint(ge=0)
    end_ticks: conint(ge=0)


class MediaAudioPeaks(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['clipmill.media.audio_peaks.v1']
    source_fingerprint: Sha256
    bucket_ticks: conint(ge=1) = Field(
        ..., description='Width of each bucket in ticks at 1/90000.'
    )
    peaks: list[Peak]
    coverage: Coverage
