# generated by datamodel-codegen:
#   filename:  clipmill.media.proxy.v1.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, conint, constr


class Audio(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    codec: str
    sample_rate: conint(ge=1)
    channels: conint(ge=1)


class Sha256(RootModel[constr(pattern=r'^sha256:[0-9a-f]{64}$')]):
    root: constr(pattern=r'^sha256:[0-9a-f]{64}$')


class Timebase(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    num: conint(ge=1)
    den: conint(ge=1)


class Coverage(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    start_ticks: conint(ge=0)
    end_ticks: conint(ge=0)


class Video(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    codec: str
    width: conint(ge=2)
    height: conint(ge=2)
    frame_rate: Timebase
    gop_frames: conint(ge=1) = Field(
        ...,
        description='Keyframe interval the encoder was pinned to; the seek granularity every scrubbing surface may assume.',
    )
    pix_fmt: str | None = None


class MediaProxy(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['clipmill.media.proxy.v1']
    source_fingerprint: Sha256
    file: constr(min_length=1) = Field(
        ..., description='Artifact-relative path of the proxy container.'
    )
    container: str | None = Field(
        None,
        description="Container format of the proxy, e.g. 'mov,mp4,m4a,3gp,3g2,mj2'.",
    )
    duration_ticks: conint(ge=0) = Field(
        ..., description='Encoded proxy duration in ticks at 1/90000.'
    )
    coverage: Coverage
    video: Video
    audio: Audio | None = Field(
        None, description='Present only when the source carries audio.'
    )
