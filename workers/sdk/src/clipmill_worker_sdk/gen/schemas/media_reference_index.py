# generated by datamodel-codegen:
#   filename:  clipmill.media.reference_index.v1.json

from __future__ import annotations

from enum import Enum
from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, conint, constr


class Kind(Enum):
    video = 'video'
    audio = 'audio'
    subtitle = 'subtitle'
    data = 'data'


class Stream(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    index: conint(ge=0)
    kind: Kind
    packet_count: conint(ge=0)
    duration_ticks: conint(ge=0) | None = None


class VideoKeyframe(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    stream_index: conint(ge=0)
    pts_ticks: int = Field(
        ..., description='Presentation tick at 1/90000 in source edit time.'
    )
    byte_pos: conint(ge=0) | None = Field(
        None,
        description='Byte position of the packet in the container when the demuxer reports one.',
    )


class Sha256(RootModel[constr(pattern=r'^sha256:[0-9a-f]{64}$')]):
    root: constr(pattern=r'^sha256:[0-9a-f]{64}$')


class Coverage(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    start_ticks: conint(ge=0)
    end_ticks: conint(ge=0)


class MediaReferenceIndex(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['clipmill.media.reference_index.v1']
    source_fingerprint: Sha256
    streams: list[Stream]
    video_keyframes: list[VideoKeyframe] = Field(
        ...,
        description='Keyframes across all video streams, ordered by (stream_index, pts_ticks).',
    )
    coverage: Coverage
