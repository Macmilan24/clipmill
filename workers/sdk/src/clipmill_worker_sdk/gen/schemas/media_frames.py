# generated by datamodel-codegen:
#   filename:  clipmill.media.frames.v1.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, conint, constr


class Frame(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    file: constr(min_length=1)
    t_ticks: conint(ge=0)


class Sha256(RootModel[constr(pattern=r'^sha256:[0-9a-f]{64}$')]):
    root: constr(pattern=r'^sha256:[0-9a-f]{64}$')


class Timebase(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    num: conint(ge=1)
    den: conint(ge=1)


class Coverage(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    start_ticks: conint(ge=0)
    end_ticks: conint(ge=0)


class MediaFrames(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['clipmill.media.frames.v1']
    source_fingerprint: Sha256
    frame_rate: Timebase = Field(
        ..., description='Nominal sampling rate, frames per second as a rational.'
    )
    frame_height: conint(ge=16) = Field(
        ...,
        description='Pixel height every frame was scaled to; width follows the display aspect.',
    )
    frames: list[Frame]
    coverage: Coverage
