# generated by datamodel-codegen:
#   filename:  clipmill.media.filmstrip.v1.json

from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, ConfigDict, Field, RootModel, conint, constr


class Tile(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    file: constr(min_length=1)
    t_ticks: conint(ge=0)


class Sha256(RootModel[constr(pattern=r'^sha256:[0-9a-f]{64}$')]):
    root: constr(pattern=r'^sha256:[0-9a-f]{64}$')


class Coverage(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    start_ticks: conint(ge=0)
    end_ticks: conint(ge=0)


class MediaFilmstrip(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['clipmill.media.filmstrip.v1']
    source_fingerprint: Sha256
    tile_height: conint(ge=16) = Field(
        ...,
        description='Pixel height every tile was scaled to; width follows the display aspect.',
    )
    interval_ticks: conint(ge=1) = Field(
        ..., description='Nominal ticks at 1/90000 between consecutive tiles.'
    )
    tiles: list[Tile]
    coverage: Coverage
