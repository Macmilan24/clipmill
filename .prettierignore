target/
dist/
node_modules/
**/gen/
pnpm-lock.yaml
LICENSE
# Contract fixtures are canonical JSON: sorted keys, two-space indent, one
# trailing newline. Their formatting is asserted byte-for-byte by the Rust and
# Python round-trip tests, so it belongs to those tests rather than to Prettier,
# which collapses short arrays onto one line and breaks the comparison.
contracts/fixtures/
# Signed Phase 0 exit evidence. These files are RFC 8785 canonical JSON covered
# by an ed25519 signature: reformatting them would not just fail the check, it
# would invalidate the attestation they exist to prove.
eval/seed40/
# The same, for the accelerated speech path's measured selection: the
# attestation is canonical JSON under an ed25519 signature taken over exactly
# those bytes.
models/attestations/
# Generated from tokens.json by packages/tokens/scripts/build-css.mjs; the
# generator owns their formatting and CI checks them for drift.
packages/tokens/tokens.css
packages/tokens/theme.css
# Vendored shadcn/ui components (MIT). Kept close to upstream so `shadcn diff`
# stays meaningful; restyling happens through the token layer, not by reflowing
# their source.
apps/desktop/src/components/ui/
