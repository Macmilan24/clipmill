/**
 * Pro-Studio Precision design tokens.
 *
 * The real token set (colors, type scale, spacing, radii, elevation for the
 * light and dark themes) is transcribed from the design in workstream W9 and
 * built into CSS variables plus a Tailwind preset. Until then this package
 * only pins its own existence so workspace lint/typecheck have a target.
 */
export const TOKENS_VERSION = '0.0.1';
