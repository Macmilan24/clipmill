/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * Every call an editorial stage made to a model, with what was sent and what came back, kept locally for diagnosis (plan, Milestone 2). This is the document to open when a proposal is strange: the exact prompt, the exact reply, the model, the route, the tokens, and on the cloud route the spend against the run's budget. It is never on the shell's readable list and never leaves the machine in an export or an archive. Credentials are not in it by construction — a call carries the request text after the transport layer, and the transport layer is where a key lives.
 */
export interface EditorialTrace {
  schema_version: "clipmill.editorial.trace.v1";
  source_fingerprint: Sha256;
  producer: {
    stage: string;
    implementation: string;
  };
  /**
   * On the cloud route: the cap the run was given and what it spent, in millionths of a US dollar so the arithmetic is exact. Absent on the local route, which spends nothing.
   */
  budget?: {
    cap_micro_usd: number;
    spent_micro_usd: number;
  };
  calls: Call[];
  /**
   * The editorial rubric selected for this run; older artifacts use interview.
   */
  content_profile?: "interview" | "scripted";
}
export interface Call {
  index: number;
  job: "propose" | "review" | "look";
  /**
   * The window a propose call was over.
   */
  window_index?: number;
  /**
   * The candidate a review or look call was about.
   */
  candidate_id?: string;
  route: "local" | "cloud";
  model: {
    name: string;
    digest?: Sha256;
    provider?: string;
  };
  prompt_version: string;
  /**
   * The prompt as the model received it, text only; frames shown to a visual model are named by `frames` rather than embedded.
   */
  request_text: string;
  frames?: {
    t_ticks: number;
  }[];
  /**
   * The reply as it came back, verbatim, even when it was malformed — especially then.
   */
  response_text?: string;
  outcome: "answered" | "none" | "malformed" | "failed" | "cancelled" | "budget";
  detail?: string;
  started_unix_millis: number;
  duration_millis: number;
  tokens: {
    input: number;
    output: number;
  };
  /**
   * What this call cost on the cloud route, from the provider's published prices at the time.
   */
  cost_micro_usd?: number;
}
