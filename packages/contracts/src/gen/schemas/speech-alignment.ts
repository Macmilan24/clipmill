/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * When each word was said, measured against the audio rather than inferred from a decoder's tokens (book ch. 13). This is the artifact every word-snapped edit in the system ultimately rests on: trims, caption cues, and the boundary optimizer all refuse to cut mid-word, and this is where 'mid-word' is defined. A stage that cannot align a span says so instead of spreading the words evenly and hoping.
 */
export interface SpeechAlignment {
  schema_version: "clipmill.speech.alignment.v1";
  source_fingerprint: Sha256;
  /**
   * The 16 kHz mono rendition scored, verified before use.
   */
  audio_artifact_id: string;
  /**
   * The text that was aligned. Alignment answers 'when', never 'what' — re-transcribing produces a new alignment rather than editing this one.
   */
  asr_artifact_id: string;
  producer: Producer;
  /**
   * The scoring stride, in ticks. Every boundary below is a multiple of it, so a consumer can state the resolution of a word edge instead of implying more precision than was measured.
   */
  frame_ticks: number;
  coverage: Coverage;
  /**
   * Ordered by time. A word appears here only if it was actually located in the audio.
   */
  words: Word[];
  /**
   * Text the aligner could not place. Listed rather than dropped, so assembly can carry the words with honest interpolated timing and mark the span, instead of silently losing what was said.
   */
  unaligned: UnalignedSpan[];
  invalid_regions: InvalidRegion[];
}
export interface Producer {
  stage: string;
  implementation: string;
  model_digest?: Sha256;
  calibration?: string;
}
export interface Coverage {
  start_ticks: number;
  end_ticks: number;
  analyzed: boolean;
  /**
   * Speech duration this pass actually placed words within.
   */
  aligned_ticks: number;
  sampling_plan?: string;
}
export interface Word {
  index: number;
  /**
   * The ASR segment this word came from.
   */
  segment_index: number;
  text: string;
  start_ticks: number;
  end_ticks: number;
  confidence: Confidence;
}
/**
 * A distribution, not a scalar: p10 is what the word's timing is worth if the model is having a bad time.
 */
export interface Confidence {
  p50: number;
  p10: number;
}
export interface UnalignedSpan {
  segment_index: number;
  /**
   * Position of this word within its utterance's text, when a single word could not be placed. Absent when the whole utterance failed, which has no single position. Assembly needs it to put the word back between its neighbours; without it, text and timing could only be re-associated by guessing.
   */
  word_index?: number;
  text: string;
  reason: "no_scoreable_text" | "out_of_vocabulary" | "score_below_threshold" | "audio_unavailable";
  detail?: string;
}
export interface InvalidRegion {
  start_ticks: number;
  end_ticks: number;
  reason: "not_analyzed" | "alignment_unavailable" | "no_audio";
  detail?: string;
}
