/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * The fan-in artifact an analyze job roots: one document naming every observation produced from a source, from the probe through the ranked set. The job store roots only a job's single final artifact and garbage collection walks recipe inputs, so this manifest's recipe lists every stage — reachability of the whole analysis follows from this one root. It is also what a shell asks for when it wants to know what a project actually has: one read instead of nine.
 */
export interface AnalysisManifest {
  schema_version: "clipmill.analysis.manifest.v1";
  source_fingerprint: Sha256;
  /**
   * Every stage that produced an artifact, in the order the DAG ran them. A stage that was skipped because the source lacked the input it needs is absent rather than present with a null: a recording with no video has no shot detection, and that is a different document from one whose shot detection failed.
   *
   * @minItems 1
   */
  stages: [Stage, ...Stage[]];
  /**
   * The span the analysis speaks for, which is the narrowest of what its stages examined. A consumer reading a candidate outside this range is reading a claim nobody made.
   */
  coverage: {
    start_ticks: number;
    end_ticks: number;
    analyzed: boolean;
  };
  /**
   * Stages the plan did not run, and why. Present so that 'this recording has no shot cuts' and 'nobody looked for shot cuts' remain distinguishable at the top level, without opening any of the artifacts below.
   */
  skipped?: SkippedStage[];
}
export interface Stage {
  kind:
    | "evidence.source_map.v1"
    | "media.ingest_manifest.v1"
    | "speech.vad.v1"
    | "speech.asr.v1"
    | "speech.alignment.v1"
    | "speech.transcript.v1"
    | "evidence.shots.v1"
    | "index.transcript.v1"
    | "discovery.candidates.v1"
    | "ranking.set.v1";
  artifact_id: Sha256;
}
export interface SkippedStage {
  kind: string;
  reason: "no_video" | "no_audio";
}
