/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * The manifest every content-addressed artifact carries. The manifest is the contract; the data files are opaque to everyone but the producing stage's schema. Deliberately excludes wall-clock time: artifact identity is a pure function of stage, inputs, config, model, and version — publish time is project state, recorded by the daemon's database.
 */
export interface ArtifactManifest {
  schema_version: "clipmill.artifact.manifest.v1";
  /**
   * Content address: hash over stage identity, ordered input hashes, canonical config, model manifest digest, and semantic version.
   */
  artifact_id: string;
  /**
   * Versioned artifact kind, e.g. 'evidence.active_speaker.v2'.
   */
  kind: string;
  /**
   * Fingerprint of the source media this artifact derives from.
   */
  source_fingerprint: string;
  /**
   * The rational timebase all times in the artifact's data files are expressed against.
   */
  timebase: {
    num: number;
    den: number;
  };
  producer: {
    /**
     * Stage identity, e.g. 'active-speaker'.
     */
    stage: string;
    /**
     * Implementation and version, e.g. 'loconet-adapter@1.4.0'.
     */
    implementation: string;
    /**
     * Digest of the model manifest, when a model produced this artifact.
     */
    model_digest?: string;
  };
  /**
   * Ordered artifact ids this artifact was derived from.
   */
  inputs: Sha256[];
  /**
   * Network policy under which this artifact was produced.
   */
  policy: "local-lock" | "network-allowed";
  /**
   * Producer-reported quality signals, e.g. {"coverage": 0.97}.
   */
  quality?: {
    [k: string]: number;
  };
  /**
   * Additive W3 cache-key metadata. Required for new daemon commits; omitted legacy v1 manifests remain readable by artifact id but cannot prove a computed cache hit.
   */
  recipe?: {
    key_version: "clipmill.artifact.key.v1";
    /**
     * Stage configuration canonicalized with RFC 8785 before hashing.
     */
    config: {};
    /**
     * Schema/runtime semantic version included in the artifact key.
     */
    semantic_version: string;
  };
  /**
   * The artifact's data files, hash-verified on read.
   *
   * @minItems 1
   */
  files: [
    {
      /**
       * Relative path within the artifact directory. The store additionally rejects traversal and absolute paths at write time.
       */
      path: string;
      sha256: Sha256;
      bytes: number;
    },
    ...{
      /**
       * Relative path within the artifact directory. The store additionally rejects traversal and absolute paths at write time.
       */
      path: string;
      sha256: Sha256;
      bytes: number;
    }[]
  ];
}
