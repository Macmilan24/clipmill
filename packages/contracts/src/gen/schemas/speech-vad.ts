/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * Where speech is, decided before anything transcribes it (book ch. 13). The first link in the speech chain: it bounds what ASR decodes, and its silence edges become candidate cut points for the boundary lattice. Every interval is integer ticks at 1/90000. Absence is explicit — a region nobody analyzed is not a region where nobody spoke.
 */
export interface SpeechVad {
  schema_version: "clipmill.speech.vad.v1";
  source_fingerprint: Sha256;
  /**
   * The 16 kHz mono rendition this pass read, verified before use.
   */
  audio_artifact_id: string;
  producer: Producer;
  /**
   * The decision parameters, recorded so a later pass knows what to change rather than guessing. They are part of the artifact key: a different threshold is a different observation, not a correction of this one.
   */
  detection: {
    /**
     * Speech probability at or above which a window counts as speech.
     */
    threshold: number;
    /**
     * Segments shorter than this are dropped rather than reported.
     */
    min_speech_ticks: number;
    /**
     * A gap shorter than this does not end a segment.
     */
    min_silence_ticks: number;
    /**
     * Padding added to each side of a segment, so a decoder is not handed a word already in progress.
     */
    speech_pad_ticks: number;
    /**
     * Samples per scored window; the resolution of every boundary below.
     */
    window_samples: number;
  };
  coverage: Coverage;
  /**
   * Total speech duration, so a consumer can state 'this recording is 8% speech' without walking the segments.
   */
  speech_ticks: number;
  /**
   * Speech, in source time, ordered and non-overlapping.
   */
  segments: SpeechSegment[];
  /**
   * The gaps between speech, including any leading and trailing silence. Carried explicitly rather than left to be derived, because these are the edges the boundary lattice is allowed to cut on and a consumer should not have to reconstruct them correctly.
   */
  silences: Interval[];
  invalid_regions: InvalidRegion[];
}
export interface Producer {
  stage: string;
  implementation: string;
  model_digest?: Sha256;
  /**
   * Which calibration mapped raw model scores onto the confidences above.
   */
  calibration?: string;
}
/**
 * What was actually examined. A skipped pass, a below-threshold result, and a genuinely empty result are three different facts, and no downstream stage may read an empty segment list as 'nobody spoke'.
 */
export interface Coverage {
  start_ticks: number;
  end_ticks: number;
  analyzed: boolean;
  sampling_plan?: string;
}
export interface SpeechSegment {
  start_ticks: number;
  end_ticks: number;
  confidence: Confidence;
}
/**
 * A distribution, not a scalar: p10 is what the segment is worth if the model is having a bad time, and downstream stages that widen uncertainty need both numbers.
 */
export interface Confidence {
  p50: number;
  p10: number;
}
export interface Interval {
  start_ticks: number;
  end_ticks: number;
}
export interface InvalidRegion {
  start_ticks: number;
  end_ticks: number;
  reason: "no_audio" | "not_analyzed" | "decode_failed";
  detail?: string;
}
