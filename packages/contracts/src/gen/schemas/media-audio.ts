/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * Descriptor for a normalized PCM audio rendition derived at ingest (book ch. 12). One schema serves both artifact kinds: media.audio_16k.v1 (16 kHz mono, the speech-model diet) and media.audio_48k.v1 (48 kHz stereo, the measurement/render diet). The artifact kind states the lineage; this document states what the bytes actually are.
 */
export interface MediaAudio {
  schema_version: "clipmill.media.audio.v1";
  source_fingerprint: Sha256;
  /**
   * Artifact-relative path of the WAV payload.
   */
  file: string;
  codec: "pcm_s16le";
  sample_rate: number;
  channels: number;
  /**
   * Frames of audio (per-channel samples), the exact payload length.
   */
  sample_count: number;
  /**
   * Rendition duration in ticks at 1/90000.
   */
  duration_ticks: number;
  coverage: Coverage;
}
export interface Coverage {
  start_ticks: number;
  end_ticks: number;
}
