/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * What a visual model saw when the review said the picture mattered (plan, Milestone 2). One check per flagged candidate over a bounded set of frames from its span, one question, a short answer — is what is being talked about visible? — and the frames it was answered from, so the answer can be walked back to pictures. Not a frame-by-frame viewer: a candidate nobody flagged has no entry here, and a candidate the model could not look at says so rather than answering.
 */
export interface EditorialLooks {
  schema_version: "clipmill.editorial.looks.v1";
  source_fingerprint: Sha256;
  inputs: {
    judgments_artifact_id: Sha256;
    frames_artifact_id: Sha256;
  };
  producer: ModelProducer;
  checks: Check[];
}
export interface ModelProducer {
  stage: string;
  implementation: string;
  route: "local" | "cloud";
  model: Model;
  prompt_version: string;
  decoding: Decoding;
}
export interface Model {
  name: string;
  digest?: Sha256;
  provider?: string;
}
export interface Decoding {
  temperature_milli: number;
  seed: number;
  max_output_tokens: number;
  constrained: boolean;
}
export interface Check {
  candidate_id: string;
  outcome: "answered" | "malformed" | "failed";
  failure?: {
    failure_class: "deterministic" | "transient" | "cancelled" | "budget";
    detail: string;
  };
  /**
   * The frames shown, by their position in the recording, so the answer can be walked back to a picture.
   *
   * @minItems 1
   */
  frames: [
    {
      t_ticks: number;
    },
    ...{
      t_ticks: number;
    }[]
  ];
  /**
   * What the model was asked about these frames, verbatim, so an answer is never read against a question nobody can see.
   */
  question: string;
  /**
   * 'yes' the thing referred to is visible; 'no' it is not; 'unclear' the frames do not settle it.
   */
  answer?: "yes" | "no" | "unclear";
  /**
   * The model's one-sentence account of what it saw.
   */
  detail?: string;
}
