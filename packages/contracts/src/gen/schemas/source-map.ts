/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * The source map built at ingest: the ONLY authority for converting between edit time and source presentation time (book ch. 10/12). All durations and offsets are integer ticks at 1/90000 against the stated timebase — variable frame rate and 29.97 drop-frame material are exactly where float-seconds pipelines drift by a frame.
 */
export interface SourceMap {
  schema_version: "clipmill.source_map.v1";
  source_fingerprint: Sha256;
  container: {
    /**
     * Container format, e.g. 'mov,mp4,m4a,3gp,3g2,mj2'.
     */
    format: string;
    /**
     * Container duration in ticks at 1/90000.
     */
    duration_ticks: number;
    /**
     * Display rotation from side data / display matrix.
     */
    rotation_deg?: 0 | 90 | 180 | 270;
  };
  /**
   * @minItems 1
   */
  streams: [
    {
      index: number;
      kind: "video" | "audio" | "subtitle" | "data";
      codec: string;
      timebase: Timebase;
      /**
       * Stream start offset in ticks at 1/90000.
       */
      start_offset_ticks?: number;
      duration_ticks?: number;
      video?: {
        coded_width: number;
        coded_height: number;
        /**
         * Width after rotation/aspect are applied — what the viewer sees.
         */
        display_width: number;
        display_height: number;
        frame_rate?: Timebase;
        /**
         * True when frame intervals vary (variable frame rate).
         */
        vfr?: boolean;
      };
      audio?: {
        sample_rate: number;
        channels: number;
      };
    },
    ...{
      index: number;
      kind: "video" | "audio" | "subtitle" | "data";
      codec: string;
      timebase: Timebase;
      /**
       * Stream start offset in ticks at 1/90000.
       */
      start_offset_ticks?: number;
      duration_ticks?: number;
      video?: {
        coded_width: number;
        coded_height: number;
        /**
         * Width after rotation/aspect are applied — what the viewer sees.
         */
        display_width: number;
        display_height: number;
        frame_rate?: Timebase;
        /**
         * True when frame intervals vary (variable frame rate).
         */
        vfr?: boolean;
      };
      audio?: {
        sample_rate: number;
        channels: number;
      };
    }[]
  ];
}
export interface Timebase {
  num: number;
  den: number;
}
