/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * Descriptor for the mezzanine proxy derived at ingest (book ch. 12): constant-frame-rate short-GOP H.264 with normalized AAC, the single decode every preview and analysis surface reads instead of the original. The descriptor states what was actually encoded, verified by probing the output, so consumers never guess.
 */
export interface MediaProxy {
  schema_version: "clipmill.media.proxy.v1";
  source_fingerprint: Sha256;
  /**
   * Artifact-relative path of the proxy container.
   */
  file: string;
  /**
   * Container format of the proxy, e.g. 'mov,mp4,m4a,3gp,3g2,mj2'.
   */
  container?: string;
  /**
   * Encoded proxy duration in ticks at 1/90000.
   */
  duration_ticks: number;
  coverage: Coverage;
  video: {
    codec: string;
    width: number;
    height: number;
    frame_rate: Timebase;
    /**
     * Keyframe interval the encoder was pinned to; the seek granularity every scrubbing surface may assume.
     */
    gop_frames: number;
    pix_fmt?: string;
  };
  /**
   * Present only when the source carries audio.
   */
  audio?: {
    codec: string;
    sample_rate: number;
    channels: number;
  };
}
/**
 * The half-open source edit-time interval this derivative covers, in ticks at 1/90000. Absence of coverage is never implicit (book ch. 13).
 */
export interface Coverage {
  start_ticks: number;
  end_ticks: number;
}
export interface Timebase {
  num: number;
  den: number;
}
