/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Lower-case hex, unprefixed, matching the sha256sum-compatible checksum file delivered alongside.
 */
export type Sha256Hex = string;

/**
 * The metadata document that ships beside a delivered clip. An export is not one file — it is the clip, two sidecars, a thumbnail, a copy of the render manifest, and this, which answers the questions an upload form asks without the user opening anything else. It restates rather than reinterprets: every number here was measured by the renderer and recorded in the render manifest, and the disclosure fields are echoed verbatim from what the user attested. A second computation would be a second chance to disagree, and on the disclosure fields a disagreement is not a bug but a false statement about the work.
 */
export interface ExportPackage {
  schema_version: "clipmill.export.package.v1";
  /**
   * The edit document this clip was rendered from.
   */
  doc_id: string;
  /**
   * The clip's own words, when it has a title. Empty when it does not — an untitled clip is a fact, not a blank to be filled with a filename.
   */
  title: string;
  /**
   * Content address of the render this was delivered from, so a package on a disk can be traced back to the artifact that produced it.
   */
  render_artifact_id: string;
  video: VideoSummary;
  audio: AudioSummary;
  disclosure: Disclosure;
  /**
   * Every file in the delivery, sorted by name so two exports of the same clip agree on order.
   */
  files: DeliveredFile[];
}
export interface VideoSummary {
  width: number;
  height: number;
  /**
   * Frame rate as a rational, never a decimal: 30000/1001 is not 29.97 and the difference is a frame per half hour.
   */
  frame_rate_num: number;
  frame_rate_den: number;
  frame_count: number;
  /**
   * Program length in ticks of the document's timebase (decision D06).
   */
  duration_ticks: number;
}
export interface AudioSummary {
  /**
   * What the render normalized to.
   */
  target_lufs: number;
  /**
   * What the finished file measures, re-decoded from the output by the renderer rather than predicted from the filter's arguments.
   */
  measured_lufs: number;
  measured_true_peak_dbtp: number;
}
/**
 * The rights claim and the model-use disclosure, echoed from what the user attested at render time. Carried in the package rather than left to be recalled at upload time, because by then the person answering the platform's question may not be the person who made the clip.
 */
export interface Disclosure {
  /**
   * What the user said the footage is, e.g. own_content.
   */
  source_attestation: string;
  /**
   * Confirmations the user gave, e.g. duration_60s for a clip past the minute mark.
   */
  gates_passed: string[];
  /**
   * Model work that shaped the footage, e.g. asr_captions or reframe. Empty for a hand-authored document.
   */
  ai_assistance: string[];
  /**
   * Whether a platform's synthetic-media question should be answered yes.
   */
  requires_ai_disclosure: boolean;
}
export interface DeliveredFile {
  /**
   * The file's name inside the export folder. A name, never a path.
   */
  name: string;
  role: "clip" | "subtitles_srt" | "subtitles_vtt" | "thumbnail" | "render_manifest" | "metadata" | "checksums";
  sha256: Sha256Hex;
  bytes: number;
}
