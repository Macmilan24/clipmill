/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * What was said, from a recognizer batched over voice activity (book ch. 13). Text and token confidence only: the intervals here are the decoder's own bookkeeping and are named as hints because word timing comes from forced alignment, never from decoder token positions. One contract serves every implementation — whisper.cpp on any CPU, an accelerated primary where one is available — so the daemon can choose between them by measured benchmark rather than by brand.
 */
export interface SpeechAsr {
  schema_version: "clipmill.speech.asr.v1";
  source_fingerprint: Sha256;
  /**
   * The 16 kHz mono rendition this pass decoded, verified before use.
   */
  audio_artifact_id: string;
  /**
   * The voice activity that bounded the decode. Recorded because re-running with different speech boundaries is a different observation.
   */
  vad_artifact_id: string;
  producer: Producer;
  /**
   * BCP 47 primary subtag of the recognized language.
   */
  language: string;
  language_confidence?: number;
  /**
   * How the text was produced. Greedy decoding at temperature zero is not a quality preference — it is what makes two runs of the same audio agree, which is what lets a cached transcript be trusted.
   */
  decoding: {
    strategy: "greedy" | "beam";
    temperature: number;
    beam_size: number;
    /**
     * Whether each window saw the previous window's text. False keeps one bad window from poisoning the rest.
     */
    conditioned_on_previous: boolean;
    /**
     * Digest of any vocabulary hint given to the decoder, so a hinted transcript is never confused with an unhinted one.
     */
    initial_prompt_digest?: string;
  };
  /**
   * Stated once, at the top, so no consumer can reach the hint fields below without having read this: the intervals in this artifact are decoder bookkeeping. Word timing is the forced aligner's output and nothing else.
   */
  timing_authority: "forced_alignment";
  coverage: Coverage;
  segments: AsrSegment[];
  /**
   * Speech the recognizer could not turn into text. Explicit, because a downstream stage reading a gap as silence would place a cut in the middle of a sentence.
   */
  invalid_regions: InvalidRegion[];
}
export interface Producer {
  stage: string;
  implementation: string;
  model_digest?: Sha256;
  calibration?: string;
}
/**
 * What was actually decoded. Speech the pass never reached is not speech that turned out to be empty.
 */
export interface Coverage {
  start_ticks: number;
  end_ticks: number;
  analyzed: boolean;
  /**
   * Speech duration actually handed to the recognizer.
   */
  decoded_ticks: number;
  sampling_plan?: string;
}
export interface AsrSegment {
  index: number;
  /**
   * Which speech segment this decode covered.
   */
  vad_segment_index: number;
  /**
   * Decoder bookkeeping, not word timing. Useful for batching and progress; never for cutting.
   */
  hint_start_ticks: number;
  /**
   * Decoder bookkeeping, not word timing.
   */
  hint_end_ticks: number;
  text: string;
  confidence: Confidence;
  /**
   * Per-token confidences, kept because a segment average hides the one hallucinated proper noun that ranking would otherwise quote.
   */
  tokens: Token[];
}
/**
 * A distribution, not a scalar: p10 is what the segment is worth if the model is having a bad time, and downstream stages that widen uncertainty need both numbers.
 */
export interface Confidence {
  p50: number;
  p10: number;
}
export interface Token {
  text: string;
  confidence: number;
}
export interface InvalidRegion {
  start_ticks: number;
  end_ticks: number;
  reason: "not_analyzed" | "decode_failed" | "below_threshold" | "no_audio";
  detail?: string;
}
