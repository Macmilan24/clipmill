/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * Sampled analysis frames decoded from the proxy for perception workers (book ch. 12/13): face detection reads these JPEGs instead of decoding video. The sampled interval is part of the recipe config, so different extractions cache independently; each frame names its exact source edit-time tick.
 */
export interface MediaFrames {
  schema_version: "clipmill.media.frames.v1";
  source_fingerprint: Sha256;
  frame_rate: Timebase;
  /**
   * Pixel height every frame was scaled to; width follows the display aspect.
   */
  frame_height: number;
  frames: {
    file: string;
    t_ticks: number;
  }[];
  coverage: Coverage;
}
/**
 * Nominal sampling rate, frames per second as a rational.
 */
export interface Timebase {
  num: number;
  den: number;
}
export interface Coverage {
  start_ticks: number;
  end_ticks: number;
}
