/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * The speech chain's single published observation: voice activity, recognized text, and measured word timing fused into one document that downstream stages read instead of the three artifacts behind it (book ch. 13, App. B). It carries what an observation is required to carry — rational intervals, a confidence distribution rather than a bare scalar, an explicit coverage statement, every producing model's identity, and the regions known to be invalid — so that discovery, ranking, captions, and the editor can request evidence by minimum quality and say why when it is missing.
 */
export interface SpeechTranscript {
  schema_version: "clipmill.speech.transcript.v1";
  source_fingerprint: Sha256;
  /**
   * The three artifacts fused here, so any claim in this document can be walked back to the pass that made it.
   */
  inputs: {
    vad_artifact_id: Sha256;
    asr_artifact_id: Sha256;
    alignment_artifact_id: Sha256;
    audio_artifact_id?: Sha256;
  };
  /**
   * One entry per contributing stage, each with the digest of the model that ran. A transcript that cannot name the models behind it cannot support the AI-use disclosure a render manifest has to make.
   */
  producers: Producer[];
  language: string;
  language_confidence?: number;
  confidence: Confidence;
  coverage: Coverage;
  /**
   * The authoritative word list, ordered by time. Every word carries timing and says how that timing was obtained.
   */
  words: Word[];
  /**
   * Recognizer segments, as index ranges into the word list rather than duplicated text, so the two can never disagree.
   */
  segments: Segment[];
  /**
   * Carried through from voice activity: these are where a cut is allowed to land without severing speech.
   */
  silences: Interval[];
  /**
   * Spans this transcript does not vouch for, including any whose word timing was interpolated rather than measured. The boundary optimizer refuses to place an edit inside one.
   */
  invalid_regions: InvalidRegion[];
}
export interface Producer {
  stage: string;
  implementation: string;
  model_digest?: Sha256;
  calibration?: string;
}
/**
 * Over the words below. Ranking reads p10, not p50, when it decides whether a quote is safe to put on screen.
 */
export interface Confidence {
  p50: number;
  p10: number;
}
/**
 * Three separate numbers because they answer three separate questions: how much of the recording was examined, how much of it was speech, and how much of that speech has timing anyone measured.
 */
export interface Coverage {
  start_ticks: number;
  end_ticks: number;
  analyzed: boolean;
  speech_ticks: number;
  aligned_ticks: number;
  sampling_plan?: string;
}
export interface Word {
  index: number;
  segment_index: number;
  text: string;
  start_ticks: number;
  end_ticks: number;
  confidence: Confidence1;
  /**
   * How the interval was obtained. 'aligned' was measured against the audio; 'interpolated' was spread across a span the aligner could not place, and every such word's span also appears in invalid_regions. Dropping the text would lose what was said; presenting it as measured would be a lie — labelling it is the only honest option.
   */
  timing: "aligned" | "interpolated";
}
export interface Confidence1 {
  p50: number;
  p10: number;
}
export interface Segment {
  index: number;
  start_ticks: number;
  end_ticks: number;
  text: string;
  first_word_index: number;
  word_count: number;
  confidence: Confidence1;
}
export interface Interval {
  start_ticks: number;
  end_ticks: number;
}
export interface InvalidRegion {
  start_ticks: number;
  end_ticks: number;
  reason: "not_analyzed" | "no_audio" | "decode_failed" | "alignment_unavailable" | "timing_interpolated";
  detail?: string;
}
