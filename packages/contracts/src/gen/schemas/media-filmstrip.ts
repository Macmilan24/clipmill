/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export type Sha256 = string;

/**
 * Timeline thumbnail tiles decoded from the proxy at a fixed cadence (book ch. 12). Each tile names its file inside the artifact and the source edit-time tick it represents, so the timeline lane never derives positions from file ordering.
 */
export interface MediaFilmstrip {
  schema_version: "clipmill.media.filmstrip.v1";
  source_fingerprint: Sha256;
  /**
   * Pixel height every tile was scaled to; width follows the display aspect.
   */
  tile_height: number;
  /**
   * Nominal ticks at 1/90000 between consecutive tiles.
   */
  interval_ticks: number;
  tiles: {
    file: string;
    t_ticks: number;
  }[];
  coverage: Coverage;
}
export interface Coverage {
  start_ticks: number;
  end_ticks: number;
}
