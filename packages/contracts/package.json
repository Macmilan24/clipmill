{
  "name": "@clipmill/contracts",
  "version": "0.0.1",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Generated contract types: protobuf-es messages + JSON Schema types with ajv validators. Never hand-edited; regenerate with `just codegen`.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p .",
    "test": "vitest run"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.2.0",
    "ajv": "^8.17.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "json-schema-to-typescript": "^15.0.0",
    "vitest": "^4.0.0"
  }
}
