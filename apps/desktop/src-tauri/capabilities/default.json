{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Window, event and lifecycle access only. The renderer reaches the daemon exclusively through this crate's own commands (daemon_state, reconnect_daemon, device_profile). The filesystem, shell and HTTP plugins are deliberately absent from Cargo.toml, so there is no capability that could grant them and no code path to re-enable them at runtime.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
