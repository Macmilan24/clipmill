{
  "name": "clipmill-workspace",
  "private": true,
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "oxlint .",
    "format": "prettier --check .",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build"
  },
  "devDependencies": {
    "oxlint": "^1.0.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.0"
  }
}
