[package]
name = "clipmill-contracts"
description = "Generated contract types (protobuf + JSON Schema). Never hand-edited; regenerate with `just codegen`"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
prost = "0.14"
regress = "0.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
