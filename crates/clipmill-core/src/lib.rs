//! Core primitives shared by every ClipMill crate.
//!
//! This crate will own identifiers, rational time (integer ticks at 1/90000 —
//! never float seconds), error taxonomy, and canonical JSON serialization.
//! Populated in workstream W1 (contracts v1); intentionally empty until then.
