# Words clippy::doc_markdown should accept in doc prose without backticks.
# ".." keeps clippy's built-in defaults.
doc-valid-idents = [
    "ClipMill", "FFmpeg", "FFprobe", "SQLite", "WebView", "OpusClip",
    "SubRip", "WebVTT",
    "..",
]
