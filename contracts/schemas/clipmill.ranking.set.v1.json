{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://clipmill.dev/schemas/clipmill.ranking.set.v1.json",
  "title": "RankingSet",
  "description": "Which nominated clips are worth showing, in what order, and cut where (book ch. 16). Three decisions live here that discovery deliberately left open: what each candidate is worth, where its boundaries should actually fall, and which subset of a cohort a user should see. Every one of them is shown rather than asserted — the score is decomposed into named factors with the evidence behind them, the chosen boundary is published beside the runner-up it beat, and a set smaller than the one requested says why instead of padding itself.",
  "type": "object",
  "required": [
    "schema_version",
    "source_fingerprint",
    "inputs",
    "producer",
    "rubric",
    "requested",
    "cohort",
    "selected",
    "shortfall"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "const": "clipmill.ranking.set.v1"
    },
    "source_fingerprint": { "$ref": "#/$defs/sha256" },
    "inputs": {
      "description": "What was read. The candidate set is the authority for every candidate id below; the index and transcript are what the factors were measured against.",
      "type": "object",
      "required": ["candidates_artifact_id", "index_artifact_id", "transcript_artifact_id"],
      "additionalProperties": false,
      "properties": {
        "candidates_artifact_id": { "$ref": "#/$defs/sha256" },
        "index_artifact_id": { "$ref": "#/$defs/sha256" },
        "transcript_artifact_id": { "$ref": "#/$defs/sha256" }
      }
    },
    "producer": { "$ref": "#/$defs/producer" },
    "rubric": {
      "description": "Which scoring rules produced these numbers. Hand-set weights calibrated against nothing yet, and named as such: the design calibrates them per genre against human labels, and until that has happened a version string is the only honest way to say which arithmetic ran. Part of the artifact key.",
      "type": "object",
      "required": ["scorer", "boundary", "selector"],
      "additionalProperties": false,
      "properties": {
        "scorer": { "type": "string", "minLength": 1 },
        "boundary": { "type": "string", "minLength": 1 },
        "selector": { "type": "string", "minLength": 1 }
      }
    },
    "requested": {
      "description": "How many clips the caller asked for, and the diversity trade-off used to pick them.",
      "type": "object",
      "required": ["count", "diversity"],
      "additionalProperties": false,
      "properties": {
        "count": { "type": "integer", "minimum": 1 },
        "diversity": {
          "description": "The lambda in maximal marginal relevance: one takes the best clips whatever their overlap, zero takes the most different ones whatever their quality.",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        }
      }
    },
    "cohort": {
      "description": "Every candidate that survived the stage-one filters, scored and ordered. Percentiles are within this list, which is what makes the displayed number an editorial index rather than a probability.",
      "type": "array",
      "items": { "$ref": "#/$defs/ranked" }
    },
    "selected": {
      "description": "The clips to show, in order. A subset of the cohort chosen for quality and difference together, so a user is not handed five versions of one moment.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^cand_[0-9a-f]{16}$"
      }
    },
    "shortfall": {
      "description": "Why fewer clips were selected than requested. Empty when the request was met. Never padded: a recording that holds three good moments should return three and say so, because the fourth would be a clip the system does not believe in.",
      "type": "array",
      "items": { "$ref": "#/$defs/shortfall_reason" }
    },
    "filtered": {
      "description": "Candidates the stage-one filters removed before scoring, with the reason. Kept so the interface can answer 'what happened to that one?' rather than the candidate simply vanishing between two documents.",
      "type": "array",
      "items": { "$ref": "#/$defs/filtered_candidate" }
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "candidate_id": {
      "type": "string",
      "pattern": "^cand_[0-9a-f]{16}$"
    },
    "interval": {
      "type": "object",
      "required": ["start_ticks", "end_ticks"],
      "additionalProperties": false,
      "properties": {
        "start_ticks": { "type": "integer", "minimum": 0 },
        "end_ticks": { "type": "integer", "minimum": 0 }
      }
    },
    "evidence_reference": {
      "type": "object",
      "required": ["kind", "index"],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["utterance", "sentence", "topic"]
        },
        "index": { "type": "integer", "minimum": 0 }
      }
    },
    "ranked": {
      "type": "object",
      "required": [
        "candidate_id",
        "rank",
        "display_score",
        "score",
        "factors",
        "uncertainty",
        "penalties",
        "boundary",
        "cluster_id"
      ],
      "additionalProperties": false,
      "properties": {
        "candidate_id": { "$ref": "#/$defs/candidate_id" },
        "rank": {
          "description": "Position in the cohort, from one. Ties are broken by candidate id, so the order does not depend on which proposer ran first.",
          "type": "integer",
          "minimum": 1
        },
        "display_score": {
          "description": "The 0-99 the interface shows: a percentile within this cohort, not a probability and not comparable across recordings. Named `display_score` rather than `score` so nobody stores it as one.",
          "type": "integer",
          "minimum": 0,
          "maximum": 99
        },
        "score": {
          "description": "The raw q(c) the percentile came from, kept so a re-tune can be reasoned about without recomputing and so two candidates a percentile rounded together can still be told apart.",
          "type": "number"
        },
        "factors": {
          "description": "The score card, decomposed. Each factor names itself, so a card that grew a factor and a card that lost one are distinguishable documents.",
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/factor" }
        },
        "uncertainty": { "$ref": "#/$defs/uncertainty" },
        "penalties": {
          "type": "array",
          "items": { "$ref": "#/$defs/penalty" }
        },
        "boundary": { "$ref": "#/$defs/boundary" },
        "cluster_id": {
          "description": "Echoed from discovery, so the interface can offer the cluster's alternatives without opening the candidate set.",
          "type": "string",
          "pattern": "^cl_[0-9a-f]{16}$"
        }
      }
    },
    "factor": {
      "description": "One axis of the score card. A factor this phase cannot measure is reported `available: false` with a stated reason and contributes nothing to the score — not zero, which would read as a measurement of badness, and not a neutral default, which would read as a measurement at all.",
      "type": "object",
      "required": ["name", "available"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "enum": [
            "hook",
            "flow",
            "value",
            "prompt_relevance",
            "novelty",
            "evidence",
            "craft",
            "feasibility"
          ]
        },
        "available": { "type": "boolean" },
        "value": {
          "description": "Present when available. Zero to one before weighting.",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "weight": {
          "description": "Present when available. What this factor contributed to q(c), before the penalties.",
          "type": "number",
          "minimum": 0
        },
        "unavailable_reason": {
          "description": "Present when not available. Why nothing measured this axis, e.g. 'no prompt was given' or 'this source carries no audio'.",
          "type": "string",
          "minLength": 1
        },
        "evidence": {
          "description": "What the factor was read from, so a user asking 'why is the hook strong?' gets the sentence rather than the number again.",
          "type": "array",
          "items": { "$ref": "#/$defs/evidence_reference" }
        }
      }
    },
    "uncertainty": {
      "description": "How much this card should be trusted, and why. Translated by the interface into words rather than shown as a hidden shading of the score.",
      "type": "object",
      "required": ["value", "band"],
      "additionalProperties": false,
      "properties": {
        "value": { "type": "number", "minimum": 0, "maximum": 1 },
        "band": {
          "type": "string",
          "enum": ["strong", "promising", "needs_review"]
        },
        "warnings": {
          "description": "Factor-specific cautions, e.g. 'word timing here was interpolated rather than measured'.",
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "penalty": {
      "type": "object",
      "required": ["reason", "value"],
      "additionalProperties": false,
      "properties": {
        "reason": {
          "description": "'repetition' is overlap with a candidate already ranked above this one. 'context_debt' is a clip that opens on something the viewer has not been told. 'rights_risk' cannot fire at this phase — there is no rights ledger — and is listed so the shape does not change when there is one.",
          "type": "string",
          "enum": ["repetition", "context_debt", "rights_risk"]
        },
        "value": { "type": "number", "exclusiveMinimum": 0 },
        "detail": { "type": "string", "minLength": 1 }
      }
    },
    "boundary": {
      "description": "Where this clip should actually be cut, chosen by scoring every legal pair in the candidate's lattice. The runner-up ships beside it because the optimizer's second choice is frequently the editor's first, and a boundary alternative one click away is cheaper than re-running the search.",
      "type": "object",
      "required": ["chosen", "score", "terms"],
      "additionalProperties": false,
      "properties": {
        "chosen": { "$ref": "#/$defs/interval" },
        "score": { "type": "number" },
        "terms": {
          "description": "The seven weighted terms behind the choice, so a boundary a user disagrees with can be argued with.",
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/boundary_term" }
        },
        "alternative": {
          "description": "The runner-up, absent only when the lattice offered exactly one legal pair.",
          "type": "object",
          "required": ["interval", "score"],
          "additionalProperties": false,
          "properties": {
            "interval": { "$ref": "#/$defs/interval" },
            "score": { "type": "number" }
          }
        },
        "considered": {
          "description": "How many legal pairs were scored. The design expects this to stay under a few hundred, which is why the search is exhaustive rather than heuristic.",
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "boundary_term": {
      "type": "object",
      "required": ["name", "value", "weight"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "enum": [
            "completeness",
            "hook",
            "payoff",
            "continuity",
            "deadair",
            "abrupt",
            "context_debt"
          ]
        },
        "value": { "type": "number", "minimum": 0, "maximum": 1 },
        "weight": {
          "description": "Signed: the first four terms reward and the last three penalise, and carrying the sign here means a reader does not have to know which is which.",
          "type": "number"
        }
      }
    },
    "shortfall_reason": {
      "type": "object",
      "required": ["reason", "count"],
      "additionalProperties": false,
      "properties": {
        "reason": {
          "type": "string",
          "enum": ["cohort_exhausted", "all_remaining_are_duplicates", "all_remaining_below_bar"]
        },
        "count": {
          "description": "How many of the requested clips this reason accounts for.",
          "type": "integer",
          "minimum": 1
        },
        "detail": { "type": "string", "minLength": 1 }
      }
    },
    "filtered_candidate": {
      "type": "object",
      "required": ["candidate_id", "reason"],
      "additionalProperties": false,
      "properties": {
        "candidate_id": { "$ref": "#/$defs/candidate_id" },
        "reason": {
          "type": "string",
          "enum": ["excluded_by_discovery", "no_legal_boundary", "below_floor"]
        },
        "detail": { "type": "string", "minLength": 1 }
      }
    },
    "producer": {
      "type": "object",
      "required": ["stage", "implementation"],
      "additionalProperties": false,
      "properties": {
        "stage": { "type": "string", "minLength": 1 },
        "implementation": { "type": "string", "minLength": 1 }
      }
    }
  }
}
