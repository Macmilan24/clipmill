{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://clipmill.dev/schemas/clipmill.artifact.manifest.v1.json",
  "title": "ArtifactManifest",
  "description": "The manifest every content-addressed artifact carries. The manifest is the contract; the data files are opaque to everyone but the producing stage's schema. Deliberately excludes wall-clock time: artifact identity is a pure function of stage, inputs, config, model, and version — publish time is project state, recorded by the daemon's database.",
  "type": "object",
  "required": [
    "schema_version",
    "artifact_id",
    "kind",
    "source_fingerprint",
    "timebase",
    "producer",
    "inputs",
    "policy",
    "files"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "const": "clipmill.artifact.manifest.v1"
    },
    "artifact_id": {
      "description": "Content address: hash over stage identity, ordered input hashes, canonical config, model manifest digest, and semantic version.",
      "$ref": "#/$defs/sha256"
    },
    "kind": {
      "description": "Versioned artifact kind, e.g. 'evidence.active_speaker.v2'.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9_.]*\\.v[0-9]+$"
    },
    "source_fingerprint": {
      "description": "Fingerprint of the source media this artifact derives from.",
      "$ref": "#/$defs/sha256"
    },
    "timebase": {
      "description": "The rational timebase all times in the artifact's data files are expressed against.",
      "type": "object",
      "required": ["num", "den"],
      "additionalProperties": false,
      "properties": {
        "num": { "type": "integer", "minimum": 1 },
        "den": { "type": "integer", "minimum": 1 }
      }
    },
    "producer": {
      "type": "object",
      "required": ["stage", "implementation"],
      "additionalProperties": false,
      "properties": {
        "stage": {
          "description": "Stage identity, e.g. 'active-speaker'.",
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "implementation": {
          "description": "Implementation and version, e.g. 'loconet-adapter@1.4.0'.",
          "type": "string"
        },
        "model_digest": {
          "description": "Digest of the model manifest, when a model produced this artifact.",
          "$ref": "#/$defs/sha256"
        }
      }
    },
    "inputs": {
      "description": "Ordered artifact ids this artifact was derived from.",
      "type": "array",
      "items": { "$ref": "#/$defs/sha256" }
    },
    "policy": {
      "description": "Network policy under which this artifact was produced.",
      "enum": ["local-lock", "network-allowed"]
    },
    "quality": {
      "description": "Producer-reported quality signals, e.g. {\"coverage\": 0.97}.",
      "type": "object",
      "additionalProperties": { "type": "number" }
    },
    "files": {
      "description": "The artifact's data files, hash-verified on read.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["path", "sha256", "bytes"],
        "additionalProperties": false,
        "properties": {
          "path": {
            "description": "Relative path within the artifact directory. The store additionally rejects traversal and absolute paths at write time.",
            "type": "string",
            "pattern": "^[^/].*$"
          },
          "sha256": { "$ref": "#/$defs/sha256" },
          "bytes": { "type": "integer", "minimum": 0 }
        }
      }
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    }
  }
}
